{
  "name": "eg",
  "private": true,
  "version": "0.0.0",
  "description": "a Sails application",
  "keywords": [],
  "dependencies": {
    "sails": "^1.0.0",
    "grunt": "1.0.1",
    "sails-hook-grunt": "^3.0.2",
    "sails-hook-orm": "^2.0.0-16",
    "sails-hook-sockets": "^1.4.0",
    "@sailshq/connect-redis": "^3.2.1",
    "@sailshq/socket.io-redis": "^5.2.0",
    "@sailshq/lodash": "^3.10.2",
    "async": "2.0.1"
  },
  "devDependencies": {
    "@sailshq/eslint": "^4.19.3"
  },
  "scripts": {
    "start": "NODE_ENV=production node app.js",
    "test": "npm run lint && npm run custom-tests && echo 'Done.'",
    "lint": "eslint . --max-warnings=0 --report-unused-disable-directives && echo '✔  Your .js files look good.'",
    "custom-tests": "echo \"(No other custom tests yet.)\" && echo",
    "docker:start": "./run.sh",
    "docker:start:mac": "docker-machine stop default && docker-machine start default && docker-machine env default && eval $(docker-machine env default) && docker-machine ip default",
    "docker:dbs": "docker exec -d mercamita_dev mongod &",
    "docker:ssh": "docker exec -it mercamita_dev bash"
  },
  "main": "app.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/daniel/eg.git"
  },
  "author": "daniel",
  "license": "",
  "engines": {
    "node": ">=8.9"
  }
}
